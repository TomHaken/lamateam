<!-- Title: `#<issue id> <summary>`, e.g. `#12 Add TC-002 task creation test`. -->

Closes #

## What and why

## How it was tested

## Checklist

- [ ] The PR closes an issue (`Closes #<id>` above), its type, priority and area labels are copied to this PR
- [ ] `npm run lint`, `npm run format:check` and `npm run typecheck` pass
- [ ] New tests are tagged with their test case ID (`@TC-XXX`), smoke tests also with `@smoke`
- [ ] Test data comes from the `testData` fixture (`autotest-<run id>-` prefix, cleaned up)
- [ ] No API token in code, logs, screenshots or this description
- [ ] README or docs updated if the behaviour changed
